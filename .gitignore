Docs/otro.pdf

# Environment variables — never commit real secrets
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
build/

# SQLite WAL temp files — runtime-only, regenerated by n8n, never meaningful to commit
n8n/data/*.sqlite-shm
n8n/data/*.sqlite-wal
