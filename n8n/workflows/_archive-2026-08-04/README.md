# Archived snapshot — 2026-08-04

Raw export of all 7 workflows pulled directly from the live n8n instance on
2026-08-04, during a session that opened the editor only to take screenshots
(see `docs/assets/Workflows/`) and tweak location copy. Not a deliberate
functional revision — kept here for reference only.

**Not canonical.** The canonical workflow definitions live in
`n8n/workflows/` (one level up), sourced from the `Emi-Integracion` branch,
which has the correct backend integration (`backend:3001/api` URLs, webhook
wiring to the real Express/Prisma backend).

If in doubt about which version to use, use the parent directory's files.
